export type ActivityRule = {
  id: number
  user_id: number
  name: string
  difficulty_rank: number
  legal_minutes: number
  legal_points: number
  goal_minutes: number
  goal_points: number
  bonus_interval_minutes: number
  bonus_points: number
  max_session_minutes: number
  default_location: string
  is_active: boolean
}

